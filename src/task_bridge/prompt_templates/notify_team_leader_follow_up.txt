请基于以上状态立即做编排动作，而不是只汇报：
1. 先判断该结果是否要求创建下一步 task。
2. 若 state=blocked 或 failed，默认必须基于返回证据创建新的 repair task，不要复用原 task。
3. 若 job 未完成，判断当前是否应继续物化下一步；必要时先检查相关 worker queue。
4. 只有以下情况可以不创建新 task：
   - 该 job 已完成，准备向用户收口；
   - 当前决定先等待其他 running task；
   - 当前缺少用户输入。
5. 最后一步，必须通过上面的飞书 chat_id 向用户发送消息，同步当前进度、关键证据、风险和下一步。

不要只复述当前结果，不要只回复 NO_REPLY。如需联系用户，使用上面的飞书 chat_id 向用户发送消息。

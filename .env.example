# Auto-loaded from the current working directory .env or ~/.openclaw/.env.
# Use the chat_id value, not TASK_BRIDGE_USER_FEISHU_ID.
TASK_BRIDGE_USER_CHAT_ID=oc_your_chat_id

# The variables below are supported by task-bridge, but are NOT auto-read from .env.
# Export them in your shell / service manager when needed.
# TASK_BRIDGE_HOME=~/.openclaw/task-bridge
# TASK_BRIDGE_CAPTURE_FILE=/tmp/task-bridge-capture.jsonl
# TASK_BRIDGE_DASHBOARD_SSH_TARGET=user@host
